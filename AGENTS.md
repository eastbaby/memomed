- 输出永远用中文。涉及专业名词可以中英都写。


- 代码尽量避免兜底逻辑，兜底返回值。尽量让逻辑清晰可追溯。
- 不允许把单个 tool 的业务特例写进通用 system prompt 或 runtime。
- 实时流和历史回放必须使用同一套事件语义，表现必须一致。
- 改 UI / streaming / HITL 后必须用真实浏览器验证关键链路。建议用/chrome 这个skills，而不要用computer use （因为用skills更稳定）。

- 当前可以不考虑历史兼容性，因为还在测试阶段，尽可能逻辑精简清晰